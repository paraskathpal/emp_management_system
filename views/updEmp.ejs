<!DOCTYPE html>
<html>
<head>    
    <title>Update Employee Record</title>
</head>
<body>
    <header>
        <h1>Update Employee Record</h1>
        <br>
        
        <div>
        <form action="/update/<%= result[0].emp_id %>" method="POST">
            <br>
            <div>
                <input type="hidden" value="<%= result[0].emp_id %>">
            </div>
            <div>
                <label for="name">Enter Employee Name</label>
                <input type="text" name="name" value="<%= result[0].name %>">
            </div>
            <br>
            <div>
                <label for="age">Enter Employee Age</label>
                <input type="number" name="age"value="<%= result[0].age %>">
            </div>
            <br>
            <div>
                <label for="dept">Enter Employee Department</label>
                <input type="text" name="dept" value="<%= result[0].department %>">
            </div>
            <br>
            <div>
                <label for="designation">Enter Employee Designation</label>
                <select name="designation" >
                    <option value="" <% if(result[0].designation == "" ) { %> selected <% } %>>--select designation--</option>
                    <option value="trainee" <% if(result[0].designation == "Trainee" ) { %> selected <% } %>>Trainee</option>
                    <option value="employee" <% if(result[0].designation == "Employee") { %> selected <% }%>>Employee</option>
                    <option value="manager" <% if(result[0].designation== "Manager" ) { %> selected <% } %>>Manager</option>
                    <option value="freelancer" <% if(result[0].designation == "Free-Lancer" ) { %> selected <% } %>>Free-Lancer</option>
                </select>
            </div>
            <br>
            <div>
                <button type="submit">Update</button>
            </div>
        </form>
        </div>

    </header>
    <br>

    <section>
        <a href="/disp"><button> Display All Employees </button></a>
        <br>
        <br>
        <div>
        <a href="/addEmployee"><button>Add New Employee</button></a>
        </div>
    </section>
</body>
</html>